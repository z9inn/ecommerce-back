<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chatapp.repository.ChatMapper">

    <select id="getAllMessages" resultType="com.example.chatapp.model.entity.ChatMessage">
        SELECT * FROM study.user_accounts;
    </select>

</mapper>